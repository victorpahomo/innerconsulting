{
  "name": "innerconsulting-backend",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "start": "npx json-server --watch db.json --port 3001 --host 0.0.0.0 --no-static",
    "dev": "npx json-server --watch db.json --port 3001 --host 0.0.0.0 --no-static"
  },
  "dependencies": {
    "json-server": "1.0.0-beta.3"
  }
}
